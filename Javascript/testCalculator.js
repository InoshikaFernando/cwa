/**
 * Create a function call 'addition' which takes 2 parameters, and return the addition of it
 * So when you call 'addition(2,3)', it should return 5
 */
function addition(x,y) {
    return x+y;
}

/**
 * Create a function call substraction, which takes 2 parameters, and returns the substraction value
 * So when you call substraction(5, 4), it should return 1
 */



function test() {
    console.log(addition(2,3))
    console.log()
}

test()